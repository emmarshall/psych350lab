---
title: "Creating Homework Checkers with psych350lab"
output: rmarkdown::html_vignette
vignette: >
  %\VignetteIndexEntry{Creating Homework Checkers with psych350lab}
  %\VignetteEngine{knitr::rmarkdown}
  %\VignetteEncoding{UTF-8}
---

## Overview

The `psych350lab` package provides interactive homework checkers that
students can use in Quarto HTML documents. Students enter their answers
into fill-in-the-blank boxes (`fitb()`) or select from dropdown menus
(`mcq()`), and the boxes turn **green** for correct or **red** for incorrect.

These checkers rely on the `webexercises` and `tinytable` packages.

## Setting Up a Quarto Document

Your Quarto HTML document needs the webexercises CSS and JS files.
Add this to your YAML header:

```yaml
  html:
    css: [webexercises.css]
    include-after-body: [webexercises.js]
```

You can get these files by running `webexercises::create_quarto_doc()` or
copying them from the webexercises package installation.

## Example 1: Descriptive Statistics Checker (Lab 06 style)

This is the most common checker — students enter Mean, SD, and SEM for
each variable and identify whether the mean is interpretable.

### Step 1: Prepare your summary statistics

```{{r}}
# Walkthrough checker
walkthrough_table <- create_univariate_checker(
  vars           = walkthrough_vars,
  stats_data     = summary_stats_walkthrough,
  label          = "num",
  binary         = "clark_grp",
  multi_category = c("type", "height_gap")
)

# Take-home checker
takehome_table <- create_univariate_checker(
  vars           = takehome_vars,
  stats_data     = summary_stats_takehome,
  multi_category = c("tomatometer", "popular")
)

```

