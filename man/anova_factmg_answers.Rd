% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/anova_factorial.R
\name{anova_factmg_answers}
\alias{anova_factmg_answers}
\title{Mixed Groups Factorial (2x2) ANOVA}
\usage{
anova_factmg_answers(
  data,
  dv_vars,
  bg_iv,
  wg_name = "Measure",
  wg_labels = NULL,
  bg_labels = NULL,
  bg_levels = NULL
)
}
\arguments{
\item{data}{A data frame in wide format (one row per subject).}

\item{dv_vars}{Character vector of length 2. Names of the DV columns
(one per WG condition).}

\item{bg_iv}{Character string. Name of the between-groups IV column.}

\item{wg_name}{Character string. Label for the within-groups factor
(default: "Measure").}

\item{wg_labels}{Character vector or NULL. Display labels for the WG
conditions. Must be same length as \code{dv_vars}. Defaults to \code{dv_vars}.}

\item{bg_labels}{Character vector or NULL. Display labels for BG levels.}

\item{bg_levels}{Numeric or character vector or NULL. Explicit ordering of
BG factor levels.}
}
\value{
A list with elements (all numeric values unrounded):
\describe{
\item{WithinSubjects}{List with \code{MainEffect_WG}, \code{Interaction}, and
\code{Error}, each containing \code{F}, \code{p_value}, \code{df}, \code{ss}, \code{ms} as applicable.}
\item{BetweenSubjects}{List with \code{MainEffect_BG} and \code{Error}, each
containing \code{F}, \code{p_value}, \code{df}, \code{ss}, \code{ms} as applicable.}
\item{Descriptives}{Tibble with cell-level descriptive statistics including
\code{bg_level}, \code{wg_level}, \code{mean}, \code{sd}, \code{n}, \code{sem}, \code{bg_label},
\code{wg_label}.}
\item{EMMs}{List with \code{BG} and \code{WG} tibbles of estimated marginal means.}
\item{FactorInfo}{List with \code{wg_name}, \code{wg_labels}, \code{dv_vars},
\code{bg_levels}, \code{bg_labels}, \code{n_obs}, \code{group_ns}.}
}
}
\description{
Performs a 2x2 mixed factorial ANOVA using the \code{jmv} package, with one
between-groups factor and one within-groups factor. Returns within-subjects
effects (WG main effect, BG x WG interaction), between-subjects effects
(BG main effect), descriptive statistics for all cells, and estimated
marginal means. All numeric values stored unrounded.
}
