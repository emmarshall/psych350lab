% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/answer_checkers.R
\name{create_univariate_checker}
\alias{create_univariate_checker}
\title{Interactive Descriptive Statistics Checker (Webexercise)}
\usage{
create_univariate_checker(
  vars,
  stats_data,
  label = NULL,
  quantitative = NULL,
  binary = NULL,
  multi_category = NULL
)
}
\arguments{
\item{vars}{Character vector. Variable names to include, in display order.}

\item{stats_data}{A data frame with columns \code{variable}, \code{mean},
\code{sd}, and \code{sem}. Typically the output of
\code{compute_summary_stats()}.}

\item{label}{Character vector or \code{NULL}. Variables that are IDs/labels.}

\item{quantitative}{Character vector or \code{NULL}. Continuous variables.}

\item{binary}{Character vector or \code{NULL}. Dichotomous variables.}

\item{multi_category}{Character vector or \code{NULL}. Nominal variables
with 3+ levels.}
}
\value{
A tibble with columns Variable, Mean, SD, SEM, and Interpretable
containing HTML strings from webexercises.
}
\description{
Creates a tibble with fill-in-the-blank inputs for mean, SD, and SEM,
plus a multiple choice dropdown for whether the mean is interpretable.
Designed for use in Quarto HTML homework checkers. Pass the result to
\code{tinytable::tt()} with \code{format_tt(escape = FALSE)} inside
a chunk with \code{results: asis} and \code{echo: false}.
}
\examples{
\dontrun{
data(superman)
library(dplyr)

walkthrough <- superman |>
  select(num, year, type, clark_height_in, clark_grp,
         height_diff, height_gap) |>
  filter(year > 1975)

stats <- compute_summary_stats(walkthrough)

tbl <- create_univariate_checker(
  vars       = names(walkthrough),
  stats_data = stats,
  label          = "num",
  binary         = "clark_grp",
  multi_category = c("type", "height_gap")
)
}

}
