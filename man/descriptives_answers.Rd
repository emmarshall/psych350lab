% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/univariate_stats.R
\name{descriptives_answers}
\alias{descriptives_answers}
\title{Descriptive Statistics Analysis}
\usage{
descriptives_answers(data, vars = NULL)
}
\arguments{
\item{data}{A data frame.}

\item{vars}{Character vector. Variable names to include in the analysis.
If \code{NULL} (default), all columns are used.}
}
\value{
A list with elements:
\describe{
\item{\code{Descriptives}}{A tibble with columns \code{variable}, \code{mean}, \code{sd},
\code{n}, and \code{sem}. All values are unrounded.}
\item{\code{Sample_Size}}{Integer. Number of rows in the (subsetted) data.}
}
}
\description{
Computes descriptive statistics for selected variables and returns a
structured results list consistent with the other \verb{*_answers()} functions
in the package. This is a convenience wrapper around
\code{\link[=compute_summary_stats]{compute_summary_stats()}} that selects the requested variables and packages
the output for use with \code{\link[=create_descriptives_checker]{create_descriptives_checker()}} and
\code{\link[=create_apa_descriptives_table]{create_apa_descriptives_table()}}.
}
\examples{
data(superman, package = "psych350data")
library(dplyr)

my_data <- superman |>
  select(num, year, clark_height_in, clark_grp, height_diff) |>
  filter(!is.na(height_diff))

result <- descriptives_answers(my_data,
  vars = c("year", "clark_height_in", "height_diff")
)
result$Descriptives
result$Sample_Size

}
\seealso{
\code{\link[=compute_summary_stats]{compute_summary_stats()}}, \code{\link[=create_descriptives_checker]{create_descriptives_checker()}},
\code{\link[=create_apa_descriptives_table]{create_apa_descriptives_table()}}
}
