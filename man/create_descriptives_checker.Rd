% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/answer_checkers.R
\name{create_descriptives_checker}
\alias{create_descriptives_checker}
\title{Interactive Descriptive Statistics Homework Checker}
\usage{
create_descriptives_checker(
  vars,
  desc_results_list,
  var_labels = NULL,
  label = NULL,
  quantitative = NULL,
  binary = NULL,
  multi_category = NULL
)
}
\arguments{
\item{vars}{Character vector. Variable names to include, in display order.}

\item{desc_results_list}{Output from \code{\link[=descriptives_answers]{descriptives_answers()}} (a list with
\verb{$Descriptives}). Also accepts a raw tibble from \code{\link[=univariate_stats_answers]{univariate_stats_answers()}}
for backwards compatibility.}

\item{var_labels}{Named character vector or \code{NULL}. Optional display labels,
e.g. \code{c(clark_height_in = "Clark Height")}.}

\item{label}{Character vector or \code{NULL}. Variables that are IDs/labels
(mean is NOT interpretable).}

\item{quantitative}{Character vector or \code{NULL}. Continuous variables
(mean IS interpretable).}

\item{binary}{Character vector or \code{NULL}. Dichotomous variables
(mean IS interpretable).}

\item{multi_category}{Character vector or \code{NULL}. Nominal variables with
3+ levels (mean is NOT interpretable).}
}
\value{
A tinytable object with embedded webexercise elements.
}
\description{
Creates a \code{\link[tinytable:tt]{tinytable::tt()}} table with fill-in-the-blank and multiple
choice inputs for checking descriptive statistics results. Requires the
\strong{tinytable} and \strong{webexercises} packages.
}
\examples{
\dontrun{
data(superman, package = "psych350data")
library(dplyr)

my_data <- superman |>
  select(year, clark_height_in, height_diff) |>
  filter(!is.na(height_diff))

result <- descriptives_answers(my_data,
  vars = c("year", "clark_height_in", "height_diff"))
create_descriptives_checker(
  vars = c("year", "clark_height_in", "height_diff"),
  desc_results_list = result,
  quantitative = c("clark_height_in", "height_diff"),
  label = "year"
)
}

}
\seealso{
\code{\link[=descriptives_answers]{descriptives_answers()}}, \code{\link[=univariate_stats_answers]{univariate_stats_answers()}}
}
