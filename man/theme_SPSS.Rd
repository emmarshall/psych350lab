% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/theme_spss.R
\name{theme_SPSS}
\alias{theme_SPSS}
\title{SPSS-Style ggplot2 Theme}
\usage{
theme_SPSS(
  base_size = 14,
  base_family = "sans",
  base_line_size = 0.75,
  base_rect_size = 0.75,
  version = "modern",
  scales = "continuous",
  scale.x = scales,
  scale.y = scales
)
}
\arguments{
\item{base_size}{Numeric. Base font size in points. Default 14.}

\item{base_family}{Character. Base font family. Default \code{"sans"} (web-safe).}

\item{base_line_size}{Numeric. Base line width. Default 0.75.}

\item{base_rect_size}{Numeric. Base rectangle border width. Default 0.75.}

\item{version}{Character. \code{"modern"} (default) for SPSS v24+ style with
white background and horizontal grid lines, or \code{"legacy"} for older SPSS
style with grey background and box border.}

\item{scales}{Character. \code{"continuous"} (default) or \code{"discrete"} for both
axes. Controls axis text sizing.}

\item{scale.x}{Character. Override scale type for x-axis only.}

\item{scale.y}{Character. Override scale type for y-axis only.}
}
\value{
A ggplot2 \code{\link[ggplot2:theme]{ggplot2::theme()}} object (complete theme).
}
\description{
A complete ggplot2 theme that mimics the appearance of SPSS charts,
optimized for HTML output. Supports both modern (v24+) and legacy
(<v24) SPSS styles.
}
\examples{
library(ggplot2)

# Basic usage with modern style
ggplot(mtcars, aes(x = wt, y = mpg)) +
  geom_point() +
  theme_SPSS()

# Legacy SPSS style
ggplot(mtcars, aes(x = wt, y = mpg)) +
  geom_point() +
  theme_SPSS(version = "legacy")

# With discrete x-axis
ggplot(mtcars, aes(x = factor(cyl), y = mpg)) +
  geom_boxplot() +
  theme_SPSS(scale.x = "discrete")

}
